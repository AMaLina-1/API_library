# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Gems required for the API library
gem "http", "~> 5.0"
gem "json", "~> 2.0"

group :development, :test do
  gem "rspec", "~> 3.0"
end